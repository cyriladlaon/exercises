import yaml
# xRead from an input file
    # xAdd entry to journal
    # xView entries
    # xSearch entries
    # Edit entries
    # Delete entries
    # xExport entries
class Journal(object):
	"""docstring for ClassName"""
	def __init__(self):
		self.birthdays_id=1
		self.entries =[]

	def read_input(self):
		docs = []
		stream = open("birthdays.yaml", "r")
		docs = yaml.load_all(stream)
		for data in docs:
			print(data)
journal = Journal()
journal.read_input()
#I'm still trying to figure out on how to solve this sir, lag brain.
